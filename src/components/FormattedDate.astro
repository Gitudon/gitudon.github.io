---
interface Props {
	date: Date;
}

const date = Astro.props.date;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("sv-SE", {
			year: "numeric",
			month: "2-digit",
			day: "2-digit",
		})
	}
</time>
